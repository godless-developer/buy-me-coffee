export type LoginTypes = {
  id: string;
  email: string;
  password: string;
  username: string;
};

export type CreatorsTypes = {
  id: number;
  name: string;
  avatar: string;
  about: string;
  socialUrl: string;
};
